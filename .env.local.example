VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Public VAPID key for Web Push. Must match VAPID_PUBLIC_KEY edge secret.
# Generate once with: npx web-push generate-vapid-keys --json
# Without this, push is silently disabled (usePushSubscription reports 'unsupported').
VITE_VAPID_PUBLIC_KEY=
